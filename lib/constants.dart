import 'package:flutter/material.dart';

// label text style
const kTextStyle = TextStyle(
  fontSize: 20,
  color: Color(0xFFb0b0b0),
);

// Large text style for highlighted
const kTextBold = TextStyle(
  fontSize: 50,
  fontWeight: FontWeight.w900,
  color: Colors.white,
);

// padding from text and the measurement
const kPaddingText = EdgeInsets.symmetric(
  vertical: 10,
  horizontal: 5,
);

// Active and inactive card colors for male and female
const Color kInActivatedCard = Color(0xFF1E1D48);
const Color kActivatedCard = Color(0xFF1D1E3);

// Bottom bar
const double kBottomBarHeight = 80.0;
const Color kBottomBarColor = Color(0xFFEB1555);

// height Slider
const Color kSliderActive = Color(0xFFEB1555);
const Color kSliderInActive = Color(0xFFBFBFBF);
const double kMin = 100;
const double kMax = 300;
